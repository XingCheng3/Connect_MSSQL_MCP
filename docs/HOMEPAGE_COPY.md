# Homepage Copy Kit (for stars & adoption)

## Repo Description (A/B)

### A (recommended)
MCP server for MSSQL with dynamic per-request connection args (read-only by default).

### B
Secure-by-default MCP bridge for Microsoft SQL Server, designed for multi-database LLM workflows.

---

## Tagline (README top one-liner)

- 中文：一个默认只读、支持动态连接参数的 MSSQL MCP Server。
- EN: A read-only-by-default MSSQL MCP server with dynamic per-request connection args.

---

## Social Post Copy

### 中文版
刚把我的 `Connect_MSSQL_MCP` 开源项目做了一轮重构：

- 默认只读，更安全
- 支持每次请求动态传入数据库连接参数
- 提供 VS Code / Cursor / Claude Desktop 等多客户端导入模板
- 已完成 npm 发布准备（可走 npx 方式接入）

欢迎试用、提 issue、点个 star 支持下 🙌

仓库地址：
https://github.com/XingCheng3/Connect_MSSQL_MCP

### English
I just shipped a major cleanup for `Connect_MSSQL_MCP`:

- Read-only by default for safer usage
- Dynamic per-request SQL Server connection args
- Multi-client MCP setup templates (VS Code / Cursor / Claude Desktop / etc.)
- npm distribution ready (npx-friendly onboarding)

Feedback, issues, and stars are very welcome 🙌

Repo:
https://github.com/XingCheng3/Connect_MSSQL_MCP

---

## Suggested Topics

`mcp`, `model-context-protocol`, `mssql`, `sqlserver`, `typescript`, `llm-tools`, `database-tools`
